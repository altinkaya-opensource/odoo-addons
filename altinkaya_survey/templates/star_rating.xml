<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- STAR RATING RESULT TEMPLATE -->
        <template id="result_star" name="Star Rating Result">
            <t t-set="graph_data" t-value="question_data['graph_data']"/>
            <t t-set="table_data" t-value="question_data['table_data']"/>
            <ul class="nav nav-tabs d-print-none" role="tablist">
                <li t-if="question_answered" class="nav-item active" >
                    <a t-att-href="'#survey_graph_question_%d' % question.id"
                        t-att-aria-controls="'survey_graph_question_%d' % question.id"
                        class="nav-link active" data-bs-toggle="tab" role="tab">
                        <i class="fa fa-bar-chart me-1"/>Graph
                    </a>
                </li>
                <li class="nav-item">
                    <a t-att-href="'#survey_data_question_%d' % question.id"
                        t-att-aria-controls="'survey_data_question_%d' % question.id"
                        t-attf-class="nav-link #{'active' if not question_answered else ''}" data-bs-toggle="tab" role="tab">
                        <i class="fa fa-list-alt me-1"/>Data
                    </a>
                </li>
            </ul>
            <div class="tab-content">
                <div t-if="question_answered" role="tabpanel" class="tab-pane active with-3d-shadow with-transitions survey_graph"
                    t-att-id="'survey_graph_question_%d' % question.id"
                    t-att-data-question-id= "question.id"
                    data-graph-type= "pie"
                    t-att-data-graph-data="graph_data">
                    <canvas style="height: 20em;"></canvas>
                </div>
                <div role="tabpanel" t-attf-class="tab-pane #{'active' if not question_answered else ''}" t-att-id="'survey_data_question_%d' % question.id">
                    <table class="table table-hover table-sm text-end">
                        <thead t-if="table_data">
                            <tr>
                                <th>Answer Choices</th>
                                <th>User Responses</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="table_data" t-as="choice_data">
                                <td>
                                    <span t-esc="choice_data['value_text']"></span>
                                </td>
                                <td class="o_survey_answer">
                                    <span t-esc="choice_data['perc_data']"></span>
                                    <span class="badge text-bg-primary" t-esc="choice_data['count']"></span>
                                    <i t-if="choice_data['count']" class="fa fa-filter text-primary filter-add-answer"
                                        t-att-data-question-id="question.id"
                                        t-att-data-row-id="choice_data['value']"
                                        role="img" aria-label="Filter surveys"
                                        title="Only show survey results having selected this answer">
                                    </i>
                                    <i t-else="" class="o_survey_answer_matrix_whitespace"/>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </template>

        <!-- STAR FORM TEMPLATE -->
        <template id="star_rating" name="Star Rating">
            <div class="form-group">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="star-rating" data-question-type="star_rating" t-att-data-name="question.id">
                            <t t-set="item_idx" t-value="0"/>
                            <t t-foreach="range(question.star_count)"
                               t-as="star_number">
                                <span class="fa fa-star-o"
                                      t-att-data-rating="(star_number+1)">
                                </span>
                            </t>

                            <input type="hidden" t-att-name="'%d' % question.id"
                                    t-att-data-question-type="question.question_type"
                                    t-att-data-data-selection-key="letters[item_idx] if useKeySelection else ''"
                                    class="rating-value" t-att-value="0"/>
                        </div>
                    </div>
                </div>
            </div>
        </template>

        <!-- STAR FORM PRINT -->
        <template id="print_star_rating" name="Star Rating Print">
            <div class="form-group">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="star-rating" data-question-type="star_rating" t-att-data-name="question.id">
                            <t t-foreach="answer.user_input_line_ids" t-as="line">
                                <t t-if="line.answer_type == 'star_rating'">
                                    <t t-foreach="range(int(line.value_star_rating))" t-as="star_number">
                                        <span class="fa fa-star" t-att-data-rating="(star_number+1)"></span>
                                    </t>
                                </t>
                            </t>
                        </div>
                    </div>
                </div>
            </div>
        </template>

        <!-- SURVEY FORM -->
        <template id="survey_form_star_rating" inherit_id="survey.question_container">
            <xpath expr="//div/t" position="after">
                <t t-if="question.question_type == 'star_rating'">
                    <t t-call="altinkaya_survey.star_rating"/>
                </t>
            </xpath>
        </template>

        <!-- SURVEY FORM PRINT -->
        <template id="survey_form_star_rating_print" inherit_id="survey.survey_page_print">
            <xpath expr="//h2" position="after">
                <t t-if="question.question_type == 'star_rating'">
                    <t t-call="altinkaya_survey.print_star_rating"/>
                </t>
            </xpath>
        </template>

        <!-- SURVEY RESULT -->
        <template id="survey_star_rating_results" inherit_id="survey.survey_page_statistics_question">
             <xpath expr="div/t[5]" position="after">
                <t t-if="question.question_type == 'star_rating'">
                    <t t-call="altinkaya_survey.result_star"/>
                </t>
            </xpath>
        </template>

    </data>
</odoo>
