<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Tree View -->
    <record id="view_auditlog_pending_tree" model="ir.ui.view">
        <field name="name">auditlog.pending.tree</field>
        <field name="model">auditlog.pending</field>
        <field name="arch" type="xml">
            <tree decoration-danger="state=='error'" decoration-muted="state=='done'">
                <field name="create_date" />
                <field name="model_name" />
                <field name="res_id" />
                <field name="method" />
                <field name="user_id" />
                <field name="state" />
                <field name="retry_count" />
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_auditlog_pending_form" model="ir.ui.view">
        <field name="name">auditlog.pending.form</field>
        <field name="model">auditlog.pending</field>
        <field name="arch" type="xml">
            <form string="Pending Audit Log">
                <header>
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="pending,processing,done,error"
                    />
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="model_name" />
                            <field name="res_id" />
                            <field name="method" />
                            <field name="log_type" />
                        </group>
                        <group>
                            <field name="user_id" />
                            <field name="create_date" />
                            <field name="retry_count" />
                            <field name="http_request_path" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Old Values" name="old_values">
                            <field
                                name="old_values_json"
                                widget="ace"
                                options="{'mode': 'json'}"
                            />
                        </page>
                        <page string="New Values" name="new_values">
                            <field
                                name="new_values_json"
                                widget="ace"
                                options="{'mode': 'json'}"
                            />
                        </page>
                        <page string="Changed Fields" name="changed_fields">
                            <field
                                name="changed_fields_json"
                                widget="ace"
                                options="{'mode': 'json'}"
                            />
                        </page>
                        <page
                            string="Error"
                            name="error"
                            attrs="{'invisible': [('state', '!=', 'error')]}"
                        >
                            <field name="error_message" />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_auditlog_pending_search" model="ir.ui.view">
        <field name="name">auditlog.pending.search</field>
        <field name="model">auditlog.pending</field>
        <field name="arch" type="xml">
            <search string="Pending Audit Logs">
                <field name="model_name" />
                <field name="res_id" />
                <field name="method" />
                <field name="user_id" />
                <filter
                    name="pending"
                    string="Pending"
                    domain="[('state', '=', 'pending')]"
                />
                <filter
                    name="processing"
                    string="Processing"
                    domain="[('state', '=', 'processing')]"
                />
                <filter name="done" string="Done" domain="[('state', '=', 'done')]" />
                <filter
                    name="error"
                    string="Error"
                    domain="[('state', '=', 'error')]"
                />
                <separator />
                <filter
                    name="today"
                    string="Today"
                    domain="[('create_date', '&gt;=', datetime.datetime.combine(context_today(), datetime.time(0,0,0)).strftime('%Y-%m-%d %H:%M:%S'))]"
                />
                <group expand="0" string="Group By">
                    <filter
                        name="group_model"
                        string="Model"
                        context="{'group_by': 'model_name'}"
                    />
                    <filter
                        name="group_method"
                        string="Method"
                        context="{'group_by': 'method'}"
                    />
                    <filter
                        name="group_state"
                        string="State"
                        context="{'group_by': 'state'}"
                    />
                    <filter
                        name="group_user"
                        string="User"
                        context="{'group_by': 'user_id'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_auditlog_pending" model="ir.actions.act_window">
        <field name="name">Pending Audit Logs</field>
        <field name="res_model">auditlog.pending</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_pending': 1}</field>
    </record>

    <!-- Menu -->
    <menuitem
        id="menu_auditlog_pending"
        name="Pending Logs"
        parent="auditlog.menu_audit"
        action="action_auditlog_pending"
        sequence="15"
    />
</odoo>
